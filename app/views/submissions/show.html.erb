<!--
This is not used yet. This could be used when showing a specific submission in detail, with a list of comments, etc.
-->

<div class="assigns">
  <div class="container">
    <h1><%= @courses.title %></h1>
    <div class="row">
      <% @assignments.each do |assignment|%>
        <div class="item col-md-4">
          <div class="img-container">
            <h2><%= assignment.title %></h2>
          </div>
          <div class="assign-info">
            <p><%= assignment.description %></p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
